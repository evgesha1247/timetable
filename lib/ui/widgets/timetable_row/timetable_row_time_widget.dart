import 'package:flutter/material.dart';

class TimetableRowTimeWidget extends StatelessWidget {
  const TimetableRowTimeWidget({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
